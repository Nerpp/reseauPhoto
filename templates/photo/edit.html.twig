{% extends 'base.html.twig' %}

{% block title %}Edit Photo{% endblock %}

{% block body %}
    <h1>Edit Photo</h1>

    {{ include('photo/_form.html.twig', {'button_label': 'Update'}) }}

    <img src="{{ asset("/img/"~photo.source) }}" class="particularPhoto" alt="...">

    
    <a href="{{ path('photo_show', {'id': photo.id }) }}">Annuler</a>

    {{ include('photo/_delete_form.html.twig') }}
{% endblock %}
