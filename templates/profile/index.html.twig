{% extends 'base.html.twig' %}

{% block title %}Profile index{% endblock %}

{% block header %}

<div class="container">
<h1>Votre profil</h1>
</div>

{% endblock %}

{% block section %}
{% if is_granted('ROLE_ADMIN') %}
    Administrator
{% elseif is_granted('ROLE_USER') %}
    User
{% else %}
    Anonymous
{% endif %}    
    <div class="container">
        <div class="">
        image de profil :
            {% if profile.profile.profile == null  %}
				<img src="{{ asset("/img/default/default.png") }}" class="rounded-circle sizeProfile" alt="...">
				{% else %}
					<img src="{{ asset("/img/"~app.user.profile.source) }}" class="rounded-circle sizeProfile" alt="...">
			{% endif %}
        </div>
        <div class="">
            <p>
                Nom : {{ profile.name }}
            </p>
        </div>
        <div class="">
        <p>
             Prénom : {{ profile.surname }}
        </p> 
        </div>
        <div class="">
            Pseudo Affichage : {{ profile.displayName }}
        </div>
    </div>
        {# {% for profile in profiles %}
            <tr>
                <td>{{ profile.id }}</td>
                <td>{{ profile.source }}</td>
                <td>{{ profile.profile ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('profile_show', {'id': profile.id}) }}">show</a>
                    <a href="{{ path('profile_edit', {'id': profile.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('profile_new') }}">Create new</a> #}
{% endblock %}
