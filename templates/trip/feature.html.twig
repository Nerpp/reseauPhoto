{% extends 'base.html.twig' %}

{% block title %}
	Folder photo select
{% endblock %}


{% block body %}

<div class="container">
<h3> Choisissez la photo de présentation en cliquant dessus</h3>
</div>

	<div class="container">
		<div class="col-3">
			<p> <a href="{{ path('trip_edit', {'id': trip.id}) }}">Retour</a></p>
		</div>
	</div>

	<div class="container">
		<div class="d-flex flex-row flex-wrap justify-content-start p-5">
			{% for trips in trip.photo %}
				<div class="col-3  p-3">
					<a href="{{ path('photo_featured', {'id': trips.id}) }}">
						<img src="{{ asset("/img/trip/"~trips.source) }}" class="photoSize" alt="...">
					</a>
				</div>
			{% endfor %}
		</div>
	</div>

{% endblock %}